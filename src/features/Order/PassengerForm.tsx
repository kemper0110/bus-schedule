import React from 'react';
import {observer} from "mobx-react-lite";

const PassengerForm = observer(function PassengerForm() {
    return (
        <div>

        </div>
    );
})

export default PassengerForm;